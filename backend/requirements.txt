# DressVibe backend — dipendenze dirette.
# pip risolve automaticamente le transitive (starlette, google-auth, anyio,
# certifi, dnspython di pymongo[srv], ecc.). Se la build su Railway fallisce
# perché una versione pinnata non esiste su PyPI, allenta quel singolo pin a
# ">=".

# --- Web framework / server ---
fastapi==0.110.1
uvicorn==0.25.0
python-multipart==0.0.28

# --- Validazione / modelli ---
pydantic==2.13.4
email-validator==2.3.0

# --- Database (MongoDB Atlas: serve l'extra [srv] -> dnspython) ---
motor==3.3.1
pymongo[srv]==4.5.0

# --- HTTP client ---
httpx==0.28.1
requests==2.34.1

# --- Auth / sicurezza ---
bcrypt==4.1.3

# --- Config ---
python-dotenv==1.2.2

# --- Immagini ---
pillow==12.2.0

# --- AI: Google Gemini (SDK ufficiale, sostituisce Emergent) ---
google-genai==2.2.0

# --- Email transazionali ---
resend==2.30.1
